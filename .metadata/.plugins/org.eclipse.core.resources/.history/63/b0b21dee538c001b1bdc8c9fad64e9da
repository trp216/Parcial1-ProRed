package view;

import java.io.IOException;
import java.util.ArrayList;

import control.PlayerController;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.*;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;
import model.Card;

public class PlayerWindow extends Stage {

	// UI Elements
	private Scene scene;
	private PlayerController control;

	@FXML
	private Button takeCardButton;
	@FXML
	private Button stand;
	@FXML
	private Label status;

	//cartas en el fxml
	@FXML
	private Label card1;
	@FXML
	private Label card2;
	@FXML
	private Label card3;
	@FXML
	private Label card4;
	@FXML
	private Label card5;




	public PlayerWindow() {

		try {
			FXMLLoader loader = new FXMLLoader(getClass().getResource("PlayerWindow.fxml"));
			Parent parent = loader.load();

			scene = new Scene(parent, 477, 422);
			this.setScene(scene);

			control = new PlayerController(this);
			
			status = (Label) loader.getNamespace().get("status");
			card1 = (Label) loader.getNamespace().get("card1");
			card2 = (Label) loader.getNamespace().get("card2");
			card3 = (Label) loader.getNamespace().get("card3");
			card4 = (Label) loader.getNamespace().get("card4");
			card5 = (Label) loader.getNamespace().get("card5");
			
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}

		
	}

	public Button getTakeCardButton() {
		return takeCardButton;
	}

	public Button getStand() {
		return stand;
	}

	public Label getStatus() {
		return status;
	}

	public void setStatus(String string) {
		// TODO Auto-generated method stub
		status.setText(string);
	}

	public void setCard(Card c) {
		if(card1.getText().equals("None")) {
			card1.setText(c.getValue()+""+c.getFigure());
		}else {
			if(card2.getText().equals("None")) {
				card2.setText(c.getValue()+""+c.getFigure());
			}
			else {
				if(card3.getText().equals("None")) {
					card3.setText(c.getValue()+""+c.getFigure());
				}
				else {
					if(card4.getText().equals("None")) {
						card4.setText(c.getValue()+""+c.getFigure());
					}
					else {
						if(card5.getText().equals("None")) {
							card5.setText(c.getValue()+""+c.getFigure());
						}
						else {
							status.setText("No puedes pedir más cartas");
						}
					}
				}
			}
		}


	}

	public void startGame() {
		card1.setText("None");
		card2.setText("None");
		card3.setText("None");
		card4.setText("None");
		card5.setText("None");
	}

}
